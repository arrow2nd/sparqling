services:
  fuseki:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3030:3030"
    volumes:
      # 開発時はデータを永続化
      - fuseki-data:/fuseki/databases
    environment:
      - FUSEKI_HOME=/opt/fuseki
      - FUSEKI_BASE=/fuseki

volumes:
  fuseki-data:
